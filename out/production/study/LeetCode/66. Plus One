class Solution {
    public int lengthOfLastWord(String s) {

        String[] splitWord = s.split(" ");
        int arrayLength = splitWord.length;

        String result = splitWord[arrayLength -1];

     return result.length();
    }
}

접근
: 단어를 분리해서 배열로 받고 그 배열의 순서 중 마지막에 있는 문자열의 길이를 확인하자!
