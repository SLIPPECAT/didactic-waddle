package TopCoder.FullSearch;
import java.util.*;

public class Cryptography2{

    public long encrypt(int[] numbers)
    {
        long ret = 1;
        Arrays.sort(numbers);
        numbers[0]
        return result;
    }
}